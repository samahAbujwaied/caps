'use strict';
const Events = require('events')
const event = new Events();

describe('CAPS', () => {
    it('All Events will return as a console ', () => {
        expect(event).toBeInstanceOf(Events)
    })})

















